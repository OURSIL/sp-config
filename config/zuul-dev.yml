spring:
  application:
    name: zuul
    
server:
  port: 3001
  
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
      
zuul:
  retryable: true

ribbon:
  ConnectTimeout: 1000
  ReadTimeout: 2000
  MaxAutoRetriesNextServer: 1
  MaxAutoRetries: 1
  
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 500
  
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
#ribbon:
#  ConnectTimeout: 1000
#  ReadTimeout: 1000
#  MaxAutoRetriesNextServer: 1
#  MaxAutoRetries: 1 
      
# zuul:
#  routes:
# item-service: "/item-service/**"  #此处配置和服务名相同时会自动配置，会从eureka拿到serviceid来配置路径
#    user-service: "/user-service/**"
#    order-service: "/order-service/**"             
